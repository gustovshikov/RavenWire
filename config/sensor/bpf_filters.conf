# BPF elephant flow exclusion filter
# Edit trusted CIDRs to match your environment before deploying.
#
# Exclude storage replication (iSCSI port 3260, NFS port 2049) from trusted storage CIDRs:
# not (host 10.0.0.0/8 and (port 3260 or port 2049))
#
# Exclude encrypted media streams (example CDN ranges):
# not (dst net 203.0.113.0/24 and (dst port 443 or dst port 8443))
#
# Uncomment and customize the line below for your environment:
# not (net 10.0.0.0/8 and (port 3260 or port 2049))
